/**
 * Prompt builders for special commands.
 * Stub implementations for this fork.
 */

export function buildInterviewPrompt(text: string): string {
  return text
}

export function buildPlanPrompt(text: string): string {
  return text
}

export function buildReviewPromptFromArgs(text: string): string {
  return text
}
